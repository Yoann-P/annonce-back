{% extends 'base.html.twig' %}


{% block body %}
	<div class="container my-5">

		<div class="row">
			<div class="col md-6">
				<img src="/uploads/{{model.illustration}}" alt={{model.name}} class="img-fluid"/>
				<p>
					<strong>{{model.price|price}}</strong>

				</p>
			</div>
			<div class="col md-6">
				<h1>{{ model.name }}</h1>

				<p>{{model.description|raw}}</p>

				<ul>

					<li>Puissance :
						{{model.dinpower}}
						ch</li>
					<li>{{model.fiscalpower}}
						CV</li>
					<li>{{model.nrj}}</li>
					<li>Année
						{{model.years}}
					</li>
					<li>{{model.gearbox}}</li>
					<li>{{model.km}}
						km</li>
				</ul>

			</div>
		</div>


	</div>
{% endblock %}
